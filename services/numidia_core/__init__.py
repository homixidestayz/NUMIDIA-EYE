"""NUMIDIA EYE - core engine: schemas, real FIRMS ingestion, processing, storage.

Data integrity is the project's rule: nothing is invented. Values below come
only from real sources (NASA FIRMS / VIIRS), the bundled geoBoundaries polygons
or Open-Meteo. Every detection carries source + fetched_at provenance.
"""
__version__ = "0.1.0"